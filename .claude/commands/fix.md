Auto-fix all linting and formatting issues in the backend project.

## Steps

Run in the `api-roma-project` directory:

1. `npm run lint:fix` — Fix linting issues with Biome
2. `npm run format` — Format code with Biome
3. `npm run type-check` — Verify no type errors remain

Report what was fixed and whether any issues remain that need manual attention.
