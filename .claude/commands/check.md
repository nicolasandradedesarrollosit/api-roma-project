Run all code quality checks for the backend project.

## Steps

Run these commands sequentially in the `api-roma-project` directory, reporting results after each:

1. **Lint**: `npm run lint`
2. **Type check**: `npm run type-check`
3. **Build**: `npm run build`

If any step fails, analyze the errors and suggest fixes. If all pass, confirm everything is clean.
